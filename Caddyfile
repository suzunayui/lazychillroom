:80

root * /srv
file_server

reverse_proxy /api/* backend:3000

# HTTPからHTTPSへのリダイレクト
redir https://{host}{uri} permanent

:443

root * /srv
file_server

reverse_proxy /api/* backend:3000

# Let's Encryptによる自動SSL証明書取得
tls {
    on_demand
}

# LazyChillRoom v7 - 本番環境設定例
# このファイルを .env.production にコピーして設定を変更してください

NODE_ENV=production
PORT=3000

# PostgreSQL データベース設定（本番環境用）
# 強力なパスワードに変更してください
DB_HOST=postgres
DB_PORT=5432
DB_USER=lazychillroom_user
DB_PASSWORD=your_secure_database_password_here
DB_NAME=lazychillroom

# Redis 設定（本番環境用）
# 強力なパスワードに変更してください
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your_secure_redis_password_here
REDIS_DB=0

# JWT設定（本番環境用 - 必ず変更してください）
# 64文字以上のランダムな文字列を生成してください
JWT_SECRET=your_very_long_and_secure_jwt_secret_key_minimum_64_characters_required

# ファイルアップロード設定
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760
ALLOWED_EXTENSIONS=jpg,jpeg,png,gif,webp,mp4,webm,pdf,txt,docx

# セキュリティ設定（本番環境用）
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100
BCRYPT_ROUNDS=12

# 本番環境用追加設定
TRUST_PROXY=true
SECURE_COOKIES=true
SESSION_SECURE=true

# SSL設定（SSL証明書がある場合）
SSL_ENABLED=false
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# ドメイン設定（SSL使用時）
DOMAIN=your-domain.com
